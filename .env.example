# Optional LOCAL development only. Production builds always use the free planner.
# Provider usage is billed to the key owner. Leave blank for zero API calls.
OPENAI_API_KEY=

# A model that supports Responses API Structured Outputs.
OPENAI_MODEL=gpt-4.1-mini

# Set true to force demo mode even when a key exists.
DEMO_MODE=false
